# Development: leave VITE_API_BASE unset (or empty).
# Vite proxies /api -> http://localhost:5000 (see vite.config.ts).
# Do NOT set VITE_API_BASE=http://localhost:5000 unless you also fix CORS/cookies.

# Production: point to your deployed API origin
# VITE_API_BASE=https://your-api.example.com
